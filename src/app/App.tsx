/**
 * Application root.
 *
 * Right now this is a placeholder that proves the toolchain is wired up.
 * Milestone M1 (`feat/design-system`) replaces it with the themed app shell,
 * the router, and the bottom navigation.
 */
export function App() {
  return (
    <main className="app-placeholder">
      <h1>second body</h1>
      <p>Foundation is in place. The coach shows up in M1.</p>
    </main>
  );
}
